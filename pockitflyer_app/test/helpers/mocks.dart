// Commonly-used mock base classes can be added here as needed.

// Register fallback values for mocktail when using any<T>() matchers with non-nullable types.
void registerCommonFallbacks() {
  // Add calls like:
  // import 'package:mocktail/mocktail.dart';
  // registerFallbackValue(YourFallbackValue());
}
